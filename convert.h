#ifndef CONVERT
#define CONVERT

#include "stdafx.h"

class Convert
{
public:
    static string ToString(int value);
    static string ToString(long value);
    static int ToInt(string value);
};

#endif // CONVERT

